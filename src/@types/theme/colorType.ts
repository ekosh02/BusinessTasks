type colorType = {
  dark: boolean;
  colors: {
    primary: string;
    background: string;
    font: string;
    border: string;
    placeholder: string;
    red: string;
    icon: string;
    card: string,
    input: {
      border: string;
      background: string;
    };
  };
};

export default colorType;
