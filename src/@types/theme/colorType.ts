type colorType = {
  dark: boolean;
  colors: {
    primary: string;
    background: string;
    font: string;
    border: string;
    placeholder: string;
    input: {
      border: string;
      background: string;
    };
  };
};

export default colorType;
